/**
 * Const values to describe mutations & actions for vuex store.
 * @param {string} ERR_NETWORK Happens when offline.
 */

export const ERR_NETWORK = 'Failed to gather data from network. Check your internet connection.';

export default [ERR_NETWORK];
